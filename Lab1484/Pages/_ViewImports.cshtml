@using Lab1484
@namespace Lab1484.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@functions {
    public static string IsActive(string page, string currentPage)
    {
        return page.Equals(currentPage, StringComparison.OrdinalIgnoreCase) ? "nav-active" : "";
    }
}